# Bubblewrap / Gradle build output — regenerated by `bubblewrap build`.
/app/
/.gradle/
/build/
gradlew
gradlew.bat
/gradle/
build.gradle
settings.gradle
gradle.properties

# Signing material — NEVER commit. Held in Play App Signing + CI secrets.
*.keystore
*.jks
*.pem
*.p12
android.keystore
signing-key-info.txt

# Bubblewrap bookkeeping
store_icon.png
twa-manifest.json.bak
